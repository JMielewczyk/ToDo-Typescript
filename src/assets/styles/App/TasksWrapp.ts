import styled from 'styled-components';

export const TasksWrapp = styled.section`
    display: grid;
    gap: 20px;
    width: 100%;
    flex-grow: 1;
    grid-auto-rows: min-content;
    overflow-y: scroll;
`;
